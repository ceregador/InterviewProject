namespace UnitTests
{
	public sealed class SpellCheckerControllerTests
    {
		/*
		 * 1. Проверяем, что метод контроллера GetWordsWithErrors возвращает ожидаемый список слов с ошибками.
		 * 2. Проверяем, что метод контроллера GetWordsWithErrors обращается к IWebAgent и к методу репозитория GetRule.
		 * 3. Проверяем, что метод контроллера GetErrorsCount возвращает ожидаемый список слов с ошибками.
		 * 4. Проверяем, что метод контроллера GetErrorsCount обращается к IWebAgent и к методу репозитория GetRule.
		 */
	}
}
