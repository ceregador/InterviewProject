namespace UnitTests
{
	public sealed class SpellCheckerControllerTests
    {
		/*
		 * 1. Check that controller's method GetWordsWithErrors returns the expected list of error words.
		 * 2. Check that controller's method GetWordsWithErrors calls IWebAgent and calls repository's method GetRule.
		 * 3. Check that controller's method GetErrorsCount returns the expected count of error words.
		 * 4. Check that controller's method GetErrorsCount calls IWebAgent and calls repository's method GetRule.
		 */
	}
}
